# Telegram Bot для ежедневных отчетов

Этот модуль реализует Telegram бота, который автоматически генерирует и отправляет ежедневные отчеты об изменениях в GitHub репозитории.

## Функциональность

- 📅 **Ежедневная отправка отчетов** в заданное время
- 📊 **Анализ изменений** в репозитории за последние 24 часа  
- 📋 **Два формата отчета**: краткая сводка (TLDR) и полный MD файл
- 🔄 **Автоматическое восстановление** после сбоев
- ⚠️ **Уведомления об ошибках** администратору

## Архитектура

Бот работает как независимый сервис и взаимодействует с сервером анализа через HTTP API:

```
Telegram Bot → HTTP API → Analysis Server → GitHub API
```

## Конфигурация

Настройка через переменные окружения в `.env` файле:

```env
# Обязательные параметры
TELEGRAM_BOT_TOKEN=your_telegram_bot_token
TELEGRAM_ADMIN_USER_ID=your_user_id

# Опциональные параметры
ANALYSIS_SERVER_URL=http://localhost:8080    # URL сервера анализа
TARGET_REPOSITORY=jetbrains/koog             # Репозиторий для анализа
DAILY_REPORT_TIME=09:00                      # Время отправки отчетов (HH:mm)
```

## Формат отчета

### Основное сообщение
```
📊 Отчет за 29.10.2024

📈 TLDR: Краткая сводка изменений

🔗 Репозиторий: jetbrains/koog
📅 Период: 2024-10-28T09:00:00Z to 2024-10-29T09:00:00Z
```

### Прикрепленный MD файл
- Подробный анализ всех изменений
- Статистика по авторам
- Список измененных файлов
- Полное описание коммитов

## Запуск

### Обычный режим
```bash
./gradlew :telegram-bot:run
```

### Тестовый режим (немедленная отправка отчета)
```bash
./gradlew :telegram-bot:run --args="--test"
```

### Справка
```bash
./gradlew :telegram-bot:run --args="--help"
```

## Компоненты

### 1. `AnalysisServerClient`
HTTP клиент для взаимодействия с сервером анализа:
- Отправка запросов на анализ репозитория
- Проверка состояния сервера
- Обработка ошибок и таймаутов

### 2. `TelegramBotService` 
Сервис для работы с Telegram Bot API:
- Отправка сообщений администратору
- Создание и отправка MD файлов
- Форматирование отчетов
- Уведомления о статусе

### 3. `DailyReportScheduler`
Планировщик ежедневных задач:
- Расчет времени до следующего выполнения
- Выполнение задач по расписанию
- Обработка ошибок и восстановление
- Возможность немедленного выполнения

### 4. `DailyReportBot`
Главный класс приложения:
- Загрузка конфигурации
- Инициализация сервисов
- Управление жизненным циклом
- Обработка сигналов завершения

## Логирование

Логи сохраняются в:
- Консоль (для разработки)
- Файл `logs/telegram-bot.log` (ротация по дням)

Уровни логирования:
- `INFO` - основные события
- `WARN` - предупреждения
- `ERROR` - ошибки и исключения

## Обработка ошибок

Бот устойчив к различным типам ошибок:

- **Недоступность сервера** - уведомление администратору
- **Отсутствие изменений** - информационное сообщение
- **Ошибки Telegram API** - повторные попытки
- **Неверная конфигурация** - завершение с ошибкой
- **Блокировка Telegram** - информирование о необходимости VPN/прокси

### Проблемы с подключением к Telegram

Если видите ошибку `UnresolvedAddressException`, это означает что не удается подключиться к `api.telegram.org`. Возможные решения:

1. **Проверьте интернет-соединение**
2. **Используйте VPN**, если Telegram заблокирован в вашем регионе  
3. **Настройте прокси** в коде (раскомментируйте строку с proxy в TelegramBotService)
4. **Проверьте DNS настройки** - попробуйте другие DNS серверы (8.8.8.8, 1.1.1.1)

## Зависимости

- `ktor-client` - HTTP клиент для Telegram API
- `kotlinx-coroutines` - асинхронность
- `kotlinx-serialization` - JSON сериализация
- `dotenv-kotlin` - загрузка переменных окружения
- `logback` - логирование